services:
  laundromat:
    build: .
    ports:
      - "80:8000"
    env_file: .env
    volumes:
      - ./data:/app/data:ro
    environment:
      - DOSSIER_DIR=/app/data/practice
    restart: unless-stopped
